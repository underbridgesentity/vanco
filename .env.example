# Set these in Vercel → Project → Settings → Environment Variables.

# Database (Neon serverless Postgres) — powers all stored data.
# Copy the pooled connection string from the Neon dashboard. Until this is set,
# the app runs in local-storage fallback mode (nothing breaks).
DATABASE_URL=

# Admin gate (Vercel serverless function: api/admin-login.js)
# The shared passcode for Vanco's team. Required to log into the admin console.
# Use a long, random value (16+ chars) — this is the main brute-force defense.
ADMIN_PASSWORD=

# Optional. Separate secret used to sign admin session tokens. If unset, the
# signing falls back to ADMIN_PASSWORD. Set a long random value for best practice.
SESSION_SECRET=

# Guest-list approval emails (Vercel serverless function: api/notify-guest.js)

# Required to actually send mail. Get a key at https://resend.com
RESEND_API_KEY=

# Optional. Sender shown on the email. Use a verified domain for production,
# e.g. "Vanco <guestlist@yourdomain.com>". Defaults to Resend's test sender.
GUEST_FROM=

# Optional. Reply-to address (e.g. management inbox).
GUEST_REPLY_TO=
